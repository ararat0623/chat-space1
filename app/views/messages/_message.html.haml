.main-chat__message-list
  - @messages.each do |message|
    .message{data: {message: {id: message.id}}}
      .message__top
        %p.message__top--name
          = message.user.name
        %p.message__top--date-time
          = message.created_at.strftime("%Y年%m月%d日 %H時%M分")

      %p.message__content
        = message.content
        %br
        = image_tag message.image, class: "message__content--image"
  